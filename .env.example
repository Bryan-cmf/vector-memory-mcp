# =============================================================================
# vector-memory-mcp — 環境變數範本
# =============================================================================
# 用法:
#   1. cp .env.example .env
#   2. 改下面的值
#   3. install.sh 會自動讀 .env (或直接 export 這些變數)
#
# ⚠️ .env 含敏感資料(API key),請加進 .gitignore,不要 commit
# =============================================================================

# ---- Qdrant ----
# 本地 Docker 模式: 留空,installer 會起容器並填 http://localhost:6333
# Qdrant Cloud 模式: 填你的 cluster URL (例: https://xxx.aws.cloud.qdrant.io:6333)
# 外部實例: 填完整 URL
QDRANT_URL=

# Qdrant API key (本地無認證模式可留空;Cloud 必填)
QDRANT_API_KEY=

# ---- Embedding 模型 ----
# 預設 BGE-m3: 1024 維、多語(中英文都強)、~2GB
# 想換小模型加速:
#   EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5   # 512 維,中文,~100MB
#   EMBEDDING_MODEL=BAAI/bge-small-en-v1.5   # 384 維,英文,~130MB
# ⚠️ 換模型後舊 collection 的向量維度會不符,要重建 collection
EMBEDDING_MODEL=BAAI/bge-m3

# ---- Collection ----
# 預設 vector_memory。可依用途分多個 (例: work / personal / project_x)
VECTOR_MEMORY_COLLECTION=vector_memory

# ---- 安裝路徑 ----
# 預設 ~/.vector-memory-mcp
# VECTOR_MEMORY_DIR=$HOME/.vector-memory-mcp
